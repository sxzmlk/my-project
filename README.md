# my-project

一个用于验证 GitHub 工作流的最小工程模板。

## 功能

- 已配置 git 用户信息与 GitHub 凭据
- 私有仓库，可随时切换为公开
- 包含基础 `.gitignore`，方便后续添加代码

## 使用

```bash
# 克隆
git clone https://github.com/sxzmlk/my-project.git

# 添加文件后提交推送
git add .
git commit -m "your message"
git push
```
